name: Build VortexCam APK

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:        # manual "Run workflow" button

jobs:
  build-apk:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable

      - name: Flutter version
        run: flutter --version

      # Generate the android/ platform scaffolding. flutter create does NOT
      # overwrite our existing lib/main.dart or pubspec.yaml.
      - name: Scaffold android project
        run: |
          rm -rf android
          flutter create --platforms=android --org com.vortexengine --project-name vortexcam .

      # Inject runtime permissions + allow cleartext (the WHIP URL is http:// on LAN)
      # and force minSdk 23 (required by flutter_webrtc).
      - name: Patch manifest + gradle
        run: |
          python3 - <<'PY'
          import re, glob, os

          # ---- AndroidManifest.xml ----
          man = "android/app/src/main/AndroidManifest.xml"
          s = open(man, encoding="utf-8").read()
          perms = [
              "android.permission.CAMERA",
              "android.permission.RECORD_AUDIO",
              "android.permission.INTERNET",
              "android.permission.ACCESS_NETWORK_STATE",
              "android.permission.FLASHLIGHT",
          ]
          inject = "".join(
              f'    <uses-permission android:name="{p}"/>\n' for p in perms
              if p not in s
          )
          if inject:
              s = s.replace("<application", inject + "    <application", 1)
          if "usesCleartextTraffic" not in s:
              s = s.replace("<application", '<application android:usesCleartextTraffic="true"', 1)
          open(man, "w", encoding="utf-8").write(s)
          print("patched manifest")

          # ---- build.gradle(.kts): force minSdk 23 ----
          for g in glob.glob("android/app/build.gradle*"):
              t = open(g, encoding="utf-8").read()
              t = t.replace("flutter.minSdkVersion", "23")
              open(g, "w", encoding="utf-8").write(t)
              print("patched", g)

          # ---- gradle.properties: unbundled ML Kit model ----
          gp = "android/gradle.properties"
          props = open(gp, encoding="utf-8").read() if os.path.exists(gp) else ""
          if "useUnbundled" not in props:
              with open(gp, "a", encoding="utf-8") as f:
                  f.write("\ndev.steenbakker.mobile_scanner.useUnbundled=true\n")
              print("patched gradle.properties: unbundled ML Kit")
          PY

      - name: Get dependencies
        run: flutter pub get

      # flutter_zxing 1.x vendors ZXing-C++ that defines utf8_t as
      # std::basic_string_view<unsigned char>. NDK 26+ removed the
      # char_traits<unsigned char> specialization from libc++ (it was never
      # in the C++ standard). The fix: change the typedef to
      # std::basic_string_view<char> — semantically identical for UTF-8.
      # We also delete any stale cmake .cxx cache so the patched header
      # is actually picked up by the NDK compiler.
      - name: Patch flutter_zxing ZXing-C++ source for NDK 26+
        run: |
          set -e
          ZXING_PKG=$(find "$HOME/.pub-cache/hosted/pub.dev" \
                          -maxdepth 1 -name "flutter_zxing-*" -type d \
                          | sort -V | tail -1)
          echo "flutter_zxing package: $ZXING_PKG"
          UTH="$ZXING_PKG/src/zxing/core/src/Utf.h"
          if [ ! -f "$UTH" ]; then
            echo "ERROR: Utf.h not found at expected path; scanning:"
            find "$HOME/.pub-cache" -name "Utf.h" 2>/dev/null
            exit 1
          fi
          sed -i 's/basic_string_view<unsigned char>/basic_string_view<char>/g' "$UTH"
          echo "Patched $UTH"
          grep -n "utf8_t\|string_view" "$UTH" | head -5
          # Remove cmake cache so NDK recompiles with the patched header.
          rm -rf "$ZXING_PKG/android/.cxx/" || true
          echo "cmake cache cleared"

      - name: Build release APK
        run: flutter build apk --release

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: vortexcam-apk
          path: build/app/outputs/flutter-apk/app-release.apk
          if-no-files-found: error
